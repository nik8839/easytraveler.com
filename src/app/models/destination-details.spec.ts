import { DestinationDetails } from './destination-details';

describe('DestinationDetails', () => {
  it('should create an instance', () => {
    expect(new DestinationDetails()).toBeTruthy();
  });
});
